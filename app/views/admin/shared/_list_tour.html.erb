<div class="container dialog" >
  <div class="col-lg-12" >
    <div class="panel panel-default" >
      <div class="panel-heading" >
        <%= t "tour_rule.list_tour" %>
      </div>
      <div class="panel-body" >
        <table class="table" >
          <tr>
            <th><%= t "tour_rule.select" %></th>
            <th><%= t "view.name_tour" %></th>
            <th><%= t "view.price_title" %></th>
            <th><%= t "view.time_tour" %></th>
            <th><%= t "view.active" %></th>
          </tr>
          <% @tours.each do |tour| %>
            <tr>
              <td><%= check_box_tag :check_rule, tour.id %></td>
              <td><%= tour.name %></td>
              <td><%= number_to_currency(tour.price) %></td>
              <th><%= tour.time_tour %> &nbsp <%= t "view.day" %></th>
              <% if tour.publish? %>
                <th><%= t "view.active_type.publish" %></th>
              <% else %>
                <th><%= t "view.active_type.expire" %></th>
              <% end %>
            </tr>
          <% end %>
          <tr>
            <%= paginate @tours, remote: true, theme: "twitter-bootstrap-3" %>
          </tr>
        </table>
      </div>
    </div>
  </div>
</div>
